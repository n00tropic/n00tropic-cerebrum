# Dependency-Track local deployment (keep out of GitHub secrets)
# Copy to .env and fill in; store securely alongside your SSH-accessible host.
# If your password/API key contains '!' and you use zsh, wrap the value in single quotes.

DT_ADMIN_PASSWORD='change-me'
DT_BASE_URL=http://localhost:8081

# Optional: API key for uploads (create in the UI after first start)
DEPENDENCY_TRACK_API_KEY='your-upload-key'
DT_API_KEY= # alias for DEPENDENCY_TRACK_API_KEY

# Syft tuning (optional)
SYFT_LOG=error
SYFT_EXCLUDE="./.git,./node_modules,./.venv,./build,./dist,./.mypy_cache,./.pytest_cache"
SYFT_YARN_ENABLE_EXPERIMENTAL_PARSER=true
