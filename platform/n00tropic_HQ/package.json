{
  "name": "n00tropic-hq",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.28.2",
  "description": "Operational HQ",
  "scripts": {
    "build": "echo 'No build artifact' && exit 0",
    "test": "echo 'No tests' && exit 0",
    "lint": "echo 'No standard lint yet' && exit 0",
    "format": "echo 'No standard format yet' && exit 0",
    "dev": "echo 'No dev server' && exit 0"
  },
  "engines": {
    "node": ">=24.11.0",
    "pnpm": ">=10.28.2"
  }
}
