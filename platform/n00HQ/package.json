{
	"name": "n00hq",
	"private": true,
	"version": "0.1.0",
	"packageManager": "pnpm@10.28.2",
	"description": "HQ / Swift Apps",
	"scripts": {
		"build": "swift build || echo 'Swift build failed'",
		"test": "swift test || echo 'Swift test failed'",
		"lint": "echo 'No standard lint yet' && exit 0",
		"format": "echo 'No standard format yet' && exit 0",
		"dev": "echo 'No dev server' && exit 0"
	},
	"engines": {
		"node": ">=24.1.0",
		"pnpm": ">=10.28.2"
	}
}
