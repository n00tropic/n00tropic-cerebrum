# n00-dashboard CLI

Wrapper for the SwiftUI control-tower companion. Use the helper script to build and run:

```bash
cd n00-dashboard
bash cli/run.sh
```

Pass any SwiftPM flags after the script (for example `--configuration release`).
