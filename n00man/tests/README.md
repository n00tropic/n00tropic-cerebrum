# n00man tests

Placeholder suite for the agent foundry. Add unit tests that validate:

- `cli/main.py` commands (scaffold/list)
- Agent profile schema expectations in `docs/agents/**`
- Integration with `scripts/enforce-doc-tags.mjs`

Run with `python -m pytest` once tests are added.
